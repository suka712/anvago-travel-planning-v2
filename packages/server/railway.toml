[build]
builder = "dockerfile"
dockerfilePath = "packages/server/Dockerfile"
watchPatterns = ["packages/server/**", "packages/shared/**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
